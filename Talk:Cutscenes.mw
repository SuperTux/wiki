To save some code, why not have both demos and cutscenes. Both would use the same format but demos wouldn't contain all the above, just the players movement. The title screen would be a great place for a real demo instead of the current 'run right and jump at random' one. - Drahcir
